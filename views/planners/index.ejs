<%- include('../partials/header') %>

<table id="list">
    <thead>
      <tr>
        <th>Plans</th>
       
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% plans.forEach(function(p) { %>
        <tr>
          <td><%= p.title %></td>
        
          <td><a href="/planners/<%= p._id %>">DETAILS</a></td>
        </tr>
        <form method="POST" action="/planners/<%= p.id%>?_method=DELETE">
            <button type="submit">X</button>
          </form>
        </li>
        <% }); %>
      </tbody>
    
    </table>
    
<%- include('../partials/footer') %>